# Minimal, dependency-free gateway image. Runs as the container's default
# non-root node user; no shell utilities beyond what the base image ships,
# no package installs at runtime.
FROM node:20-alpine

WORKDIR /app
COPY package.json server.js ./

ENV GATEWAY_PORT=8787
EXPOSE 8787

USER node
CMD ["node", "server.js"]
